# 프로젝트 지시사항

## 개발 환경 설정
- **프로젝트 경로**: [프로젝트 루트 경로]
- **개발 도구**: [사용하는 IDE/에디터]

## 코드 품질 관리

### 문법 오류 확인 및 수정
**코드 변경 후 반드시 `get_errors` 도구로 문법 오류 확인:**
- 코드 수정 시 `get_errors` 도구 사용
- VSCode workspace 통합으로 실시간 문법 오류 파악 가능
- 발견된 오류는 즉시 수정
- import 누락, 타입 불일치 등 모든 컴파일 에러 해결

**예시:**
```
1. 코드 수정 완료
2. get_errors 실행하여 에러 확인
3. 에러 발견 시 즉시 수정
4. 다시 get_errors로 검증
5. 에러 없을 때까지 반복
```

## Tool 사용 원칙 (중요!) ⚠️

### 실행 가능한 질문은 반드시 도구 사용
사용자가 실행 가능한 작업을 요청할 때는 **반드시 도구를 사용해서 실행**합니다.

**잘못된 예 (절대 금지!):**
```
User: "디스크 남은용량은?"
Assistant: "명령어를 사용하세요..." ❌
```

**올바른 예:**
```
User: "디스크 남은용량은?"
Assistant: [run_in_terminal 도구 실행] ✅
결과: "남은 용량: 150GB"
```

**도구 사용이 필요한 경우:**
- 파일 시스템 정보 조회 (용량, 파일 목록 등)
- 시스템 정보 확인 (프로세스, 메모리 등)
- 코드 실행 및 결과 확인
- 파일 읽기/쓰기/수정
- 디렉토리 탐색
- Git 작업
- 빌드/테스트 실행

**명령어만 알려주는 경우 (예외):**
- 사용자가 "명령어만 알려줘"라고 명시적으로 요청
- 복잡한 설정이 필요해서 단계별 설명이 필요한 경우

**원칙:**
1. **실행 가능하면 → 도구 사용**
2. 설명만 필요하면 → 답변만
3. 애매하면 → 도구 사용 (더 안전)

## 최근 완료 작업

[여기에 최근 완료된 주요 작업들을 기록]

## 진행 중인 작업

[현재 진행 중인 작업 및 다음 단계]

## 알려진 이슈

[발견된 문제점 및 해결 방법]


## 최근 세션 자동 기록
- 날짜: 2025-11-13
- 작업: 원격 저장소와 동기화 시도 (`git pull`)
- 결과: 실패. 시스템 프록시 설정으로 인해 `git pull`이 실패했습니다. 오류: "Invalid proxy URL 'http://pac.score/score.pac': only SOCKS proxies support paths".
- 다음 단계: 프록시 환경을 확인하거나, 프록시를 무시하도록 Git 설정을 조정한 후 다시 시도합니다.
